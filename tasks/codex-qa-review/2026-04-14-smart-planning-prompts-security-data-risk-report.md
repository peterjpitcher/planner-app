- `SEC-001 (High)` Yes, the new routes need auth checks, and the spec never says so. The spec adds four new API routes but does not require `getAuthContext()`/`getServerSession(authOptions)` or a `401` on missing session, even though this repo’s Supabase service-role client bypasses RLS and existing routes rely on explicit session gating plus `user_id` scoping as the security boundary. Refs: [spec route table](</Users/peterpitcher/Cursor/OJ-Planner2.0/docs/superpowers/specs/2026-04-14-smart-planning-prompts-design.md:123>), [auth helper](</Users/peterpitcher/Cursor/OJ-Planner2.0/src/lib/authServer.js:24>), [service-role client](</Users/peterpitcher/Cursor/OJ-Planner2.0/src/lib/supabaseServiceRole.js:5>), [existing route pattern](</Users/peterpitcher/Cursor/OJ-Planner2.0/src/app/api/areas/route.js:7>).

- `SEC-002 (High)` `/api/planning-sessions` is missing an ownership model. The spec says “Finish Planning” writes to `planning_sessions`, but never says the server must derive `user_id` from `session.user.id`, ignore any client-supplied `user_id`, `id`, `created_at`, or `completed_at`, or derive/validate `window_type` and `window_date` server-side. In a service-role route, a naive `insert(request.json())` would let a caller create planning rows for another user or forge arbitrary windows. Refs: [planning_sessions schema](</Users/peterpitcher/Cursor/OJ-Planner2.0/docs/superpowers/specs/2026-04-14-smart-planning-prompts-design.md:25>), [Finish Planning write](</Users/peterpitcher/Cursor/OJ-Planner2.0/docs/superpowers/specs/2026-04-14-smart-planning-prompts-design.md:89>), [route table](</Users/peterpitcher/Cursor/OJ-Planner2.0/docs/superpowers/specs/2026-04-14-smart-planning-prompts-design.md:127>), [service-role client](</Users/peterpitcher/Cursor/OJ-Planner2.0/src/lib/supabaseServiceRole.js:5>).

- `SEC-003 (High)` `/api/user-settings` has the same cross-user read/write risk. The spec introduces a “simple form” backed by `user_settings`, but never requires GET/PUT to be hard-pinned to `session.user.id` or says `user_id`/`id` are immutable server-side. If the handler accepts identifiers from query/body while using the service-role client, one user can read or overwrite another user’s planning-window settings. Refs: [user_settings schema](</Users/peterpitcher/Cursor/OJ-Planner2.0/docs/superpowers/specs/2026-04-14-smart-planning-prompts-design.md:36>), [settings section](</Users/peterpitcher/Cursor/OJ-Planner2.0/docs/superpowers/specs/2026-04-14-smart-planning-prompts-design.md:105>), [route table](</Users/peterpitcher/Cursor/OJ-Planner2.0/docs/superpowers/specs/2026-04-14-smart-planning-prompts-design.md:129>), [existing user-scoped route pattern](</Users/peterpitcher/Cursor/OJ-Planner2.0/src/app/api/areas/route.js:15>).

- `SEC-004 (Medium)` The spec does not mention Zod or any equivalent request validation for the new endpoints. That leaves `PUT /api/user-settings` and `POST /api/planning-sessions` open to malformed or hostile payloads: non-time strings, zero-length windows like `start=end`, unexpected keys such as `user_id`, invalid `window_type`, arbitrary `window_date`, or client-controlled timestamps. Even if Postgres rejects some bad values, that becomes uncontrolled 500/error-path behavior instead of a clean 400 contract. Refs: [settings inputs](</Users/peterpitcher/Cursor/OJ-Planner2.0/docs/superpowers/specs/2026-04-14-smart-planning-prompts-design.md:107>), [route table](</Users/peterpitcher/Cursor/OJ-Planner2.0/docs/superpowers/specs/2026-04-14-smart-planning-prompts-design.md:123>), [existing validator pattern](</Users/peterpitcher/Cursor/OJ-Planner2.0/src/lib/validators.js:62>).

- `SEC-005 (Medium)` `planning_sessions` has no uniqueness guarantee for one row per `user_id + window_type + window_date`. The hook is supposed to check whether a matching session exists, but the schema does not define a composite unique constraint or idempotent write behavior. Retries, double-clicks, and multi-device submits can create duplicate rows and ambiguous “already planned” state. Refs: [planning_sessions schema](</Users/peterpitcher/Cursor/OJ-Planner2.0/docs/superpowers/specs/2026-04-14-smart-planning-prompts-design.md:25>), [hook existence check](</Users/peterpitcher/Cursor/OJ-Planner2.0/docs/superpowers/specs/2026-04-14-smart-planning-prompts-design.md:118>), [Sunday dual-write behavior](</Users/peterpitcher/Cursor/OJ-Planner2.0/docs/superpowers/specs/2026-04-14-smart-planning-prompts-design.md:92>).

No separate SEC item for check 6: the current task mutation path already has auth and ownership enforcement, so reusing `updateTask` is not a new bypass today. Refs: [PATCH `/api/tasks`](</Users/peterpitcher/Cursor/OJ-Planner2.0/src/app/api/tasks/route.js:178>), [task ownership pre-read](</Users/peterpitcher/Cursor/OJ-Planner2.0/src/services/taskService.js:123>), [final update scoped by `user_id`](</Users/peterpitcher/Cursor/OJ-Planner2.0/src/services/taskService.js:224>).