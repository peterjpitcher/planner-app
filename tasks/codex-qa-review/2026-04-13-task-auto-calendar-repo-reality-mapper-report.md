I read `docs/superpowers/specs/2026-04-13-task-automation-calendar-view-design.md` first. Repo reality:

**1. Cron Infrastructure**
- Exists: [src/app/api/cron/daily-task-email/route.js](/Users/peterpitcher/Cursor/OJ-Planner2.0/src/app/api/cron/daily-task-email/route.js:83), lines 83-219, exports `GET(request)` for a daily digest cron. [src/services/dailyTaskEmailService.js](/Users/peterpitcher/Cursor/OJ-Planner2.0/src/services/dailyTaskEmailService.js:72), lines 72-220, provides user resolution, task query, and email rendering. [vercel.json](/Users/peterpitcher/Cursor/OJ-Planner2.0/vercel.json:3), lines 3-15, schedules `/api/cron/daily-task-email` at `0 7 * * *` and `0 8 * * *`, plus `/api/cron/office365-sync` every minute. [supabase/migrations/20260121_create_daily_task_email_runs.sql](/Users/peterpitcher/Cursor/OJ-Planner2.0/supabase/migrations/20260121_create_daily_task_email_runs.sql:1), lines 1-20, creates the existing idempotency table.
- Pattern: Auth/time checks are split into `isVercelCronRequest()`, `getBooleanSearchParam()`, `getDigestTimeZone()`, `isTimeWindowInZone()`, `claimDailyRun()`, and `updateDailyRun()` in `route.js:7-81`. `GET()` resolves one digest user via `resolveDigestUserId({ supabase, email })`, fetches tasks via `fetchOutstandingTasks({ supabase, userId, todayDateKey })`, builds content via `buildDailyTaskEmail(...)`, then calls `sendMicrosoftEmail(...)`.
- Gotchas: This cron does not mutate tasks at all. It only selects `tasks` where `state = 'today'`, `due_date IS NOT NULL`, and `due_date <= today` in [dailyTaskEmailService.js](/Users/peterpitcher/Cursor/OJ-Planner2.0/src/services/dailyTaskEmailService.js:110), lines 110-116. Auth is not simply “`x-vercel-cron` or `CRON_SECRET`”: if `CRON_SECRET` exists, the route requires `x-cron-secret` unless a manual token is used (`route.js:98-105`). It also has manual `dryRun` / `force` controls and can skip outside an env-driven send window (`route.js:107-135`).
- Spec diff: The spec assumes new `POST /api/cron/demote-today-tasks` and `POST /api/cron/demote-week-tasks`; those files do not exist. There is also no `cron_runs` table in the repo today. Current cron behavior is digest-only, `GET`-based, 07:00/08:00 UTC, and uses `daily_task_email_runs` keyed by `(user_id, run_date)`, not `(operation, run_date)`. Recipient/from addresses are env-driven, not hardcoded to `peter@orangejelly.co.uk`.

**2. Email Sending**
- Exists: [src/lib/microsoftGraph.js](/Users/peterpitcher/Cursor/OJ-Planner2.0/src/lib/microsoftGraph.js:14), lines 14-89, exports `getMicrosoftGraphAccessToken()` and `sendMicrosoftEmail({ fromUser, to, subject, html, text })`. The separate user-connection flow lives in [src/services/office365ConnectionService.js](/Users/peterpitcher/Cursor/OJ-Planner2.0/src/services/office365ConnectionService.js:98), lines 98-155, and shared Microsoft OAuth helpers live in [src/lib/office365/oauth.js](/Users/peterpitcher/Cursor/OJ-Planner2.0/src/lib/office365/oauth.js:22), lines 22-42.
- Pattern: `getMicrosoftGraphAccessToken()` uses client credentials from `MICROSOFT_TENANT_ID`, `MICROSOFT_CLIENT_ID`, and `MICROSOFT_CLIENT_SECRET`, requests `scope=https://graph.microsoft.com/.default`, and caches the token in process memory (`microsoftGraph.js:14-52`). `sendMicrosoftEmail()` then POSTs to `https://graph.microsoft.com/v1.0/users/{fromUser}/sendMail` with either HTML or text (`microsoftGraph.js:54-89`).
- Gotchas: `sendMicrosoftEmail()` does not read `office365_connections`, refresh tokens, or user access tokens. The per-user Office365 connection is only for Todo sync. By direct code evidence, the Office365 OAuth scopes are only `Tasks.ReadWrite` in `office365/oauth.js:34-42`; by inference, mail sending depends on Azure app permissions on the shared app registration, not on the user’s Office365 integration status.
- Spec diff: The spec treats `sendMicrosoftEmail()` as part of the broader Office365 setup. In reality, email sending is app-level and independent of the per-user Office365 task connection. Office365 connection is required for Todo sync, but task mutations swallow sync failures in [src/services/taskService.js](/Users/peterpitcher/Cursor/OJ-Planner2.0/src/services/taskService.js:112), lines 112-118 and 244-250, so task updates still succeed when not connected.

**3. Task State Management**
- Exists: [src/app/api/tasks/[id]/route.js](/Users/peterpitcher/Cursor/OJ-Planner2.0/src/app/api/tasks/%5Bid%5D/route.js:8), lines 8-53, handles `PATCH(request, { params })` with auth, rate limiting, and delegation to `updateTask(...)`. The actual state logic is in [src/services/taskService.js](/Users/peterpitcher/Cursor/OJ-Planner2.0/src/services/taskService.js:123), lines 123-252. The DB trigger is in [supabase/migrations/20260404000001_prioritisation_replacement.sql](/Users/peterpitcher/Cursor/OJ-Planner2.0/supabase/migrations/20260404000001_prioritisation_replacement.sql:171), lines 171-196.
- Pattern: `updateTask({ supabase, userId, taskId, updates, options = {} })` fetches the existing row, whitelists update fields, validates project ownership, applies state-transition rules, validates the merged row, updates the DB, touches related projects, and then best-effort syncs to Office365. The trigger `fn_task_state_cleanup()` is `BEFORE INSERT OR UPDATE` and enforces state invariants.
- Gotchas: The trigger does more than clear `today_section`. It also sets `completed_at` when entering `done`, clears `completed_at` when leaving `done`, and sets `entered_state_at` whenever state changes (`migration:174-189`). The service layer duplicates some of that: it also sets `entered_state_at`, sets/clears `completed_at`, and deletes `today_section` from outbound updates when leaving `today` (`taskService.js:181-212`). `sort_order` is not patchable here; reorder uses separate batch RPC.
- Spec diff: The spec is only partially right about `fn_task_state_cleanup`; it is not a “clear `today_section` only” trigger. Also, although `PATCH /api/tasks/[id]` exists, the main client path is actually `PATCH /api/tasks` via [src/lib/apiClient.js](/Users/peterpitcher/Cursor/OJ-Planner2.0/src/lib/apiClient.js:169), lines 169-185, not the `[id]` route.

**4. Sorting**
- Exists: [src/lib/taskSort.js](/Users/peterpitcher/Cursor/OJ-Planner2.0/src/lib/taskSort.js:7), lines 7-63, defines `compareTasksByDueDateAsc`, `compareTasksBySortOrderAsc`, `sortTasksByDueDateAsc`, and `sortTasksBySortOrderAsc`. [src/lib/sortOrder.js](/Users/peterpitcher/Cursor/OJ-Planner2.0/src/lib/sortOrder.js:9), lines 9-36, defines `computeSortOrder(above, below)`, `needsReindex(above, below)`, and `reindex(items)`.
- Pattern: `compareTasksByDueDateAsc()` sorts due date, then `created_at`, then `name`, then `id`. `compareTasksBySortOrderAsc()` sorts `sort_order`, then `created_at`, then `name`, then `id`. The actual Plan backlog load path is [src/app/api/tasks/route.js](/Users/peterpitcher/Cursor/OJ-Planner2.0/src/app/api/tasks/route.js:89), lines 89-95, which orders tasks by `sort_order ASC, created_at ASC`, and [src/components/plan/PlanBoard.jsx](/Users/peterpitcher/Cursor/OJ-Planner2.0/src/components/plan/PlanBoard.jsx:222), lines 222-295, which fetches backlog in pages and preserves returned order. Same-column DnD reorder updates local array order and persists batch sort_order in `PlanBoard.jsx:493-525`.
- Gotchas: `PlanBoard.jsx` does not import or call `compareTasksBySortOrderAsc` or any other function from `taskSort.js`. The backlog column renderer in [src/components/plan/BoardColumn.jsx](/Users/peterpitcher/Cursor/OJ-Planner2.0/src/components/plan/BoardColumn.jsx:446), lines 446-475, only filters; it does not sort. `taskSort.js` is currently used in Projects, e.g. [src/components/Projects/ProjectItem.js](/Users/peterpitcher/Cursor/OJ-Planner2.0/src/components/Projects/ProjectItem.js:16), lines 16 and 28-33.
- Spec diff: The spec assumes backlog currently uses `compareTasksBySortOrderAsc` inside `PlanBoard.jsx` and can be swapped there. In reality, backlog order is API-driven plus local DnD order, so adding `compareBacklogTasks()` only to `taskSort.js` and wiring PlanBoard superficially would not affect the current fetch/render path.

**5. Navigation**
- Exists: [src/components/layout/TabBar.jsx](/Users/peterpitcher/Cursor/OJ-Planner2.0/src/components/layout/TabBar.jsx:8), lines 8-13, defines tabs `Today`, `Plan`, `Projects`, `Ideas`. [src/components/layout/Sidebar.jsx](/Users/peterpitcher/Cursor/OJ-Planner2.0/src/components/layout/Sidebar.jsx:18), lines 18-25, defines sidebar items `Today`, `Plan`, `Projects`, `Ideas`, `Reports`, `Integrations`.
- Pattern: Both components use a top-level array and `.map()` it into links. `TabBar` uses Heroicons outline icons and renders desktop top tabs plus mobile bottom tabs (`TabBar.jsx:15-78`). `Sidebar` uses `lucide-react` icons and a separate left-nav pattern with sign-out footer (`Sidebar.jsx:27-99`).
- Gotchas: Adding a new nav item is at least two edits. The icon libraries differ, so the same icon import will not fit both files. Active-state matching also differs slightly between the two components.
- Spec diff: There is no `/calendar` page today, and no calendar item in either nav. The spec’s `CalendarDaysIcon` fits the Heroicons-based `TabBar`, but `Sidebar` would need a Lucide equivalent. The sidebar also has extra items beyond the spec’s simplified tab set.

**6. DnD Infrastructure**
- Exists: [package.json](/Users/peterpitcher/Cursor/OJ-Planner2.0/package.json:13), lines 13-16, has `@dnd-kit/core ^6.3.1`, `@dnd-kit/sortable ^10.0.0`, `@dnd-kit/utilities ^3.2.2`. [src/components/today/TodayView.jsx](/Users/peterpitcher/Cursor/OJ-Planner2.0/src/components/today/TodayView.jsx:105), lines 105-107 and 525-529, sets up `DndContext` with `PointerSensor` and `closestCenter`. [src/components/shared/TaskCard.jsx](/Users/peterpitcher/Cursor/OJ-Planner2.0/src/components/shared/TaskCard.jsx:151), lines 151-195, makes each task sortable via `useSortable({ id: task.id })` with listeners on a drag handle.
- Pattern: TodayView’s `handleDragEnd` in [TodayView.jsx](/Users/peterpitcher/Cursor/OJ-Planner2.0/src/components/today/TodayView.jsx:317), lines 317-406, derives source and target sections from `over.id`, uses `arrayMove` and `sort_order` recomputation for within-section reorders, and PATCHes `state: 'today', today_section` for cross-section moves. Sortable containers are created in [src/components/today/TodaySection.jsx](/Users/peterpitcher/Cursor/OJ-Planner2.0/src/components/today/TodaySection.jsx:61), lines 61-85. The Plan board has a separate, richer DnD setup with keyboard sensor, drag overlay, and `useDroppable` columns in [src/components/plan/PlanBoard.jsx](/Users/peterpitcher/Cursor/OJ-Planner2.0/src/components/plan/PlanBoard.jsx:455), lines 455-458 and 607-612, plus [BoardColumn.jsx](/Users/peterpitcher/Cursor/OJ-Planner2.0/src/components/plan/BoardColumn.jsx:240), line 240.
- Gotchas: Current DnD is list/column oriented, not date-cell oriented. TodayView has no `DragOverlay`, no keyboard sensor, and no explicit day-like droppable targets. TaskCard assumes sortable context and only exposes drag listeners on the handle, so a calendar pill/day-cell system would need new droppable wrappers and likely new lightweight draggable components.
- Spec diff: The spec assumes existing DnD infra is close to month/day calendar dropping. It is not. No calendar route/components exist, no edge/month-hover navigation exists, and there is no current mobile branch that disables DnD for a calendar while switching to a picker workflow.

**7. Task Data Shape**
- Exists: [src/services/taskService.js](/Users/peterpitcher/Cursor/OJ-Planner2.0/src/services/taskService.js:38), line 38, defines `TASK_SELECT_FIELDS` as `id, name, description, due_date, state, today_section, sort_order, area, task_type, chips, waiting_reason, follow_up_date, project_id, user_id, completed_at, entered_state_at, source_idea_id, created_at, updated_at`. `createTask()` selects `${TASK_SELECT_FIELDS}, projects(id, name)` (`taskService.js:93-97`), `updateTask()` selects only `TASK_SELECT_FIELDS` (`224-230`), and `fetchTaskById()` also selects only `TASK_SELECT_FIELDS` (`323-335`).
- Pattern: List fetching is actually in [src/app/api/tasks/route.js](/Users/peterpitcher/Cursor/OJ-Planner2.0/src/app/api/tasks/route.js:63), lines 63-125, not in `taskService.js`. That GET joins `projects(id, name, area)`, supports `projectId`, `state`, `states`, `completedSince`, `limit`, and `offset`, orders by `sort_order` then `created_at`, and flattens `project_name` / `project_area` onto each row (`104-109`). The client-facing fetch helpers are in [src/lib/apiClient.js](/Users/peterpitcher/Cursor/OJ-Planner2.0/src/lib/apiClient.js:87), lines 87-127.
- Gotchas: Response shape is inconsistent by path. GET list rows include nested `projects` and top-level `project_name` / `project_area`; `updateTask()` returns no project join; `createTask()` joins only `projects(id, name)`. GET `/api/tasks` is paginated with default limit 100, and PlanBoard fetches backlog only 20 at a time in [PlanBoard.jsx](/Users/peterpitcher/Cursor/OJ-Planner2.0/src/components/plan/PlanBoard.jsx:35), lines 35 and 241-245. GET also kicks off background Office365 auto-sync when enabled (`api/tasks/route.js:35-49`).
- Spec diff: The spec says “use existing GET /api/tasks” to fetch all non-done tasks. The API can support that logically, but not in a single guaranteed unpaginated call; full-calendar loading would need pagination or `apiClient.getAllTasks()`. The spec also assumes a simple task-fetch path, but current GET has Office365 auto-sync side effects and returns a richer shape than `taskService.js` alone suggests.