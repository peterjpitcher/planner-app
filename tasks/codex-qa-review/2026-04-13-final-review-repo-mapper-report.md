- [src/lib/cronAuth.js](/Users/peterpitcher/Cursor/OJ-Planner2.0/src/lib/cronAuth.js:17) — High: cron auth is checking `x-cron-secret` / `x-vercel-cron`, but Vercel’s documented cron contract uses `Authorization: Bearer ${CRON_SECRET}`. With a normal Vercel `CRON_SECRET` setup, these routes will 401 and never run. Fix: read `request.headers.get('authorization')` and compare it to `Bearer ${process.env.CRON_SECRET}`; do not rely on `x-cron-secret` as the primary production path.

- [src/app/api/cron/demote-today-tasks/route.js](/Users/peterpitcher/Cursor/OJ-Planner2.0/src/app/api/cron/demote-today-tasks/route.js:37) and [src/app/api/cron/demote-week-tasks/route.js](/Users/peterpitcher/Cursor/OJ-Planner2.0/src/app/api/cron/demote-week-tasks/route.js:44) — High: both routes claim a `cron_runs` row before `resolveDigestUserId()`, but the top-level `catch` never marks that row failed. Any exception after the claim and before the later status update leaves the row stuck in `claimed`, and every retry for that date becomes `already_run`. Fix: keep `runId` in outer scope and, in the outer `catch`, call `updateCronRun(...status: 'failed'...)` before returning 500.

- [src/app/api/cron/demote-today-tasks/route.js](/Users/peterpitcher/Cursor/OJ-Planner2.0/src/app/api/cron/demote-today-tasks/route.js:83) and [src/app/api/cron/demote-week-tasks/route.js](/Users/peterpitcher/Cursor/OJ-Planner2.0/src/app/api/cron/demote-week-tasks/route.js:90) — Medium: per-task update failures are silently ignored. `demotedCount` drops, but the email body still lists every fetched task, and the run is still marked `success` unless email sending fails. Fix: track successful vs failed updates, email only successfully moved tasks, and mark the run `partial`/`failed` when any `updateTask` call returns an error.

- [src/components/calendar/CalendarView.jsx](/Users/peterpitcher/Cursor/OJ-Planner2.0/src/components/calendar/CalendarView.jsx:122) — High: moving a task from the calendar via the context menu removes it from local state instead of updating it in place. The calendar loads `today,this_week,backlog,waiting`, so moved tasks should still be visible; right now they disappear until a full reload. Fix: update the task object locally with the new `state`/`today_section` instead of filtering it out.

- [src/components/plan/PlanBoard.jsx](/Users/peterpitcher/Cursor/OJ-Planner2.0/src/components/plan/PlanBoard.jsx:250) — Medium: the new backlog due-date sort is only applied to fetched pages. Pagination still comes from `/api/tasks` in `sort_order` order, cross-column moves prepend into backlog unsorted, and manual reorders can violate the new rule. Result: backlog order is wrong as soon as there are more than 20 items or tasks move into backlog. Fix: move the primary due-date ordering into the API query before pagination, and re-apply `compareBacklogTasks` on every backlog mutation.

Cron auth reference: [Managing Cron Jobs](https://vercel.com/docs/cron-jobs/manage-cron-jobs), [Cron Jobs](https://vercel.com/docs/cron-jobs).