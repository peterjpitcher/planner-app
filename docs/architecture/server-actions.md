---
generated: true
last_updated: 2026-04-21T00:00:00Z
source: session-setup
project: planner-app
---

# Server Actions

> Auto-generated by session-setup. Manual edits will be overwritten on next refresh.

## Status: Not Used

This project does **not** use Next.js Server Actions (`'use server'`). No files contain the `'use server'` directive.

All data mutations are performed through **API routes** (`src/app/api/`) called from client components via the `useApiClient` hook (`src/hooks/useApiClient.js`) or direct `fetch()` calls.

### Data Flow Pattern

```
Client Component
  → useApiClient().apiCall('/api/tasks', { method: 'POST', body: ... })
    → API Route handler (getAuthContext + Supabase query)
      → JSON response
```

See [routes.md](./routes.md) for the full API route table.
