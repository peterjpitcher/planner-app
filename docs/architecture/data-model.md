---
generated: true
last_updated: 2026-04-21T00:00:00Z
source: session-setup
project: planner-app
---

# Data Model

> Auto-generated by session-setup. Manual edits will be overwritten on next refresh.

See session-context.md for full schema. This file will be populated by the database agent.

## Tables Referenced in Code

The following tables are accessed by API routes (discovered via `.from('table')` grep):

| Table | Primary API Routes |
|-------|--------------------|
| `tasks` | `/api/tasks`, `/api/tasks/[id]`, `/api/tasks/batch`, `/api/tasks/sort-order`, `/api/planning-candidates`, `/api/completed-items`, `/api/cron/demote-today-tasks`, `/api/cron/demote-week-tasks` |
| `projects` | `/api/projects`, `/api/projects/[id]`, `/api/completed-items`, `/api/areas`, `/api/notes`, `/api/admin/migrate` |
| `notes` | `/api/notes`, `/api/notes/batch`, `/api/completed-items` |
| `journal_entries` | `/api/journal/entries`, `/api/journal/entries/cleanup` |
| `user_settings` | `/api/user-settings` |
| `planning_sessions` | `/api/planning-sessions` |
| `office365_connections` | `/api/cron/office365-sync` |
| `daily_task_email_runs` | `/api/cron/daily-task-email` |

## Key Relationships (from CLAUDE.md)

- `tasks.projectId` -> `projects.id` (FK)
- `notes.projectId` -> `projects.id` (FK)
- `notes.taskId` -> `tasks.id` (FK)
- All tables have `user_id` for ownership
